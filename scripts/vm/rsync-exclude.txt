# What `vm.py sync` leaves at home: build output, runtime data and this tool's
# own local state.
#
# Never exclude a TRACKED source path here. The box builds its evidence hash
# from what it received; an exclude over tracked source would make the artifact
# describe a tree the box never had. apps/server/data is safe — its only
# tracked entry is an empty .gitkeep. .git stays: run.sh reads head and
# tree_hash from it.
.cache
.turbo
dist
node_modules
target
.venv
__pycache__
*.pyc
.pytest_cache
.ruff_cache
coverage
apps/web/test-results
apps/web/playwright-report
apps/web/playwright/.cache
apps/server/data
backups
.dt-xdg
.vm
.ah-out
# Per-host, gitignored, and holding the hypervisor token: a throwaway VM has no
# business carrying the credential that can clone and destroy it, and `bake`
# would write it into the template. vm.py reads this file on the CLIENT.
.claude/settings.local.json
# Same class: per-host and gitignored. It carries AH_TEST_DB and the dev box's
# heavy-free AH_REQUIRED — and verify.sh sources it wherever it finds it, so a
# box that received it inherits a required set without the heavy steps and can
# pass --strict while skipping them. Without the file AH_REQUIRED stays unset
# and run.sh's box rule makes every step of a heavy layer mandatory (R-0048).
.devenv.sh
# Leftovers from before the migration. Still excluded for the same reason they
# are still gitignored: .crabbox/captures holds unredacted failure bundles, and
# a sync would put them on every box and into the next template. They go when
# the two directories go.
.crabbox
.crabbox-out
